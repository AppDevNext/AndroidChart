jdk:
 - openjdk17
install:
 - ./gradlew :chartLib:publishToMavenLocal :chartLibCompose:publishToMavenLocal -Pgroup=com.github.AppDevNext.AndroidChart -PRELEASE_SIGNING_ENABLED=false -x test
 - find . -name "*.aar"
